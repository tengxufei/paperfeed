{
  "_about": "PaperFeed config. Keys starting with _ are comments and are ignored.",
  "_keyword_sets": "One block per topic you follow. Every field except 'name' is optional, but a set needs a 'query' (preferred) or at least one of terms / all_of / authors.",
  "_query": "What this topic is, written as a boolean expression: AND / OR / NOT in capitals, \"quoted phrases\", brackets to group, and title: abstract: author: journal: mesh: to say where to look. Run 'python3 paperfeed.py query --cheatsheet' for the syntax, and 'python3 paperfeed.py query \"...\"' to try one out before saving it. The shape that works is: group your synonyms, then AND the groups together - each bracketed group counts as one concept when ranking.",
  "keyword_sets": [
    {
      "name": "de novo protein design",
      "query": "(\"de novo protein design\" OR RFdiffusion OR ProteinMPNN) AND (binder OR \"binding affinity\" OR experimental)",
      "_terms": "The older style, still supported. OR'ed together: a paper matching ANY of these is a hit, searched as exact phrases in title+abstract. If a set has both, 'query' wins. Run 'python3 paperfeed.py migrate-queries' to convert these into a query.",
      "terms": [],
      "_all_of": "Every term here must ALSO appear. Use it to narrow a noisy set, e.g. terms=[binder] + all_of=[protein].",
      "all_of": [],
      "_authors": "Follow specific people. Written as PubMed writes them: surname then initials. Papers by them get a scoring bonus. Note the search APIs are loose - 'Baker D' will also return papers by 'Baker DA' - but only an exact initial match earns the bonus.",
      "authors": [],
      "_exclude": "Drop papers mentioning any of these, for this set only.",
      "exclude": [],
      "_fields": "'title_abstract' (default, less noise) or 'all' (searches everywhere, including MeSH and full text).",
      "fields": "title_abstract",
      "_journals": "allow = ONLY these journals. deny = never these. Matched as substrings, so 'Nature' covers 'Nature Methods'.",
      "journals": {
        "allow": [],
        "deny": []
      },
      "_sources": "Override the global sources for this set only, e.g. {\"pubmed\": false} to make it preprints-only.",
      "_min_score": "Override the global ranking.min_score for this set. Useful for a broad term: a title match is worth 4, so min_score 4 means the paper must be ABOUT this, not merely mention it. Hidden papers are reported and not marked as seen.",
      "_require_title_groups": "A blunter noise filter than min_score: how many of your bracketed concepts must appear in the TITLE, not just the abstract. 0 (the default) means no such requirement; 1 means the paper must be at least partly about your topic; 2 on a two-concept query means it must be about both.",
      "require_title_groups": 0,
      "enabled": true
    },
    {
      "_comment": "A tighter example. mesh: is a precision layer for PubMed, but MeSH headings are only assigned once a paper has been indexed - 73% of the newest papers have none, and preprints never do. So put it in an OR alongside plain words, never on its own.",
      "name": "IDH1 in glioma",
      "query": "(IDH1 OR \"isocitrate dehydrogenase 1\" OR \"IDH-mutant\" OR mesh:\"Isocitrate Dehydrogenase\") AND (glioma OR glioblastoma OR astrocytoma)",
      "require_title_groups": 1,
      "enabled": true
    }
  ],
  "_ranking": "How papers are ordered. Scoring works per CONCEPT - each bracketed group in your query - not per word, so writing one idea three ways (glioma OR glioblastoma OR astrocytoma) does not score three times. A title match is weighted above two abstract mentions on purpose: the title is what a paper is ABOUT. recency_bonus is deliberately small: inside a 14-day window almost everything is recent, so it is a tiebreaker, not a driver. Set enabled=false to go back to plain date order. min_score hides weak matches (try 2 or 3). Citation counts never affect this score.",
  "ranking": {
    "enabled": true,
    "title_weight": 4.0,
    "abstract_weight": 1.0,
    "breadth_bonus": 1.0,
    "recency_bonus": 1.0,
    "author_bonus": 3.0,
    "min_score": 0.0
  },
  "_mute": "Applies to every keyword set. Useful for an ambiguous word: 'binder' matches battery-electrode papers, so muting 'cathode' cleans that up. Muted papers are NOT remembered, so removing a mute brings them back.",
  "mute": {
    "terms": [],
    "journals": []
  },
  "_collect": "The collector makes your library grow on its own. Every run, papers scoring at least min_score are saved automatically, up to max_per_run. Papers you click Save on are marked 'you'; these are marked 'auto', so you can always tell them apart. Turn it off and the library only grows when you click.",
  "collect": {
    "enabled": true,
    "min_score": 6.0,
    "max_per_run": 25
  },
  "_ai": "OPTIONAL and off by default. Everything else in PaperFeed - including every dashboard and chart - works with no key at all. The key is read from an environment variable, exactly like the email password: export PAPERFEED_AI_KEY='...' in your shell. It is never stored in this file and never typed into a web page.",
  "_ai_provider": "'anthropic' for the Claude API, or 'openai' for OpenAI and anything that speaks its chat-completions API - Groq, DeepSeek, Together, OpenRouter, LM Studio, Ollama. For those, also set base_url, e.g. 'https://api.groq.com/openai/v1' or 'http://localhost:11434/v1' for a local model.",
  "ai": {
    "enabled": false,
    "provider": "anthropic",
    "base_url": "",
    "model": "claude-haiku-4-5",
    "api_key_env": "PAPERFEED_AI_KEY",
    "_interests": "Free text. The more specific, the better the triage.",
    "interests": "",
    "max_papers_per_run": 40,
    "batch_size": 10
  },
  "_trends": "OPTIONAL. Every interval_days, re-queries the last few months and writes a themed briefing. Uses the same provider and key as 'ai', with its own model.",
  "trends": {
    "enabled": false,
    "months": 3,
    "max_papers": 300,
    "model": "claude-sonnet-5",
    "interval_days": 30,
    "email": false
  },
  "_interval_days": "Minimum days between digests. The scheduler wakes the script daily; it exits quietly until this many days have passed.",
  "interval_days": 3,
  "_lookback_days": "How far back each run searches. Keep comfortably larger than interval_days.",
  "lookback_days": 14,
  "_max_per_set": "Cap on papers fetched per keyword set per source.",
  "max_per_set": 100,
  "sources": {
    "pubmed": true,
    "preprints": true
  },
  "_contact_email": "NCBI asks that automated tools identify themselves. Sent as a User-Agent header only.",
  "contact_email": "you@example.com",
  "_email": "Set enabled to true once your digests look right. The password is NEVER stored here: set the environment variable named in password_env. For Gmail, use an app password.",
  "email": {
    "enabled": false,
    "smtp_host": "smtp.gmail.com",
    "smtp_port": 465,
    "username": "you@example.com",
    "password_env": "PAPERFEED_SMTP_PASSWORD",
    "from_address": "you@example.com",
    "to_addresses": [
      "you@example.com"
    ],
    "subject_prefix": "[PaperFeed]",
    "_send_when_empty": "Email you even when there is nothing new. Off by default; the digest file is written either way.",
    "send_when_empty": false
  }
}